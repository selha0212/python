# 파이썬 타입 : int, float, str, bool, tuple, list, dict, set
# 파이썬으로 타입 자체를 만들 수 있다 -> 사용자 정의 타입 : class
# 학생관리
    # 이름, 학번, 생일, 연락처 ...... 데이터
    # 입학, 전학, 휴학, 퇴학   ...... 기능    -> 함수

str = 'hello'
# 파이썬의 모든 타입은 클래스다
# 파이썬의 모든 변수는 객체다
print(type(str))
print(len(str))